spring:
  profiles:
    active: dev
    
pagehelper:
  autoDialect: true
  closeConn: true
  pageSizeZero: true
  
server:
  session.timeout: 900
  context-path: /ymcake

---
spring:
  profiles: dev
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/owcake?useSSL=false
    username: owcake
    password: Pass1234